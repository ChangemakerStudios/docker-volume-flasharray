run:
  timeout: 5m
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - misspell
    - gocritic
    - revive
issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck, gocritic]
